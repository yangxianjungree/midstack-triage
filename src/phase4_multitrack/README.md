# Legacy Phase 4 Multitrack

This directory only keeps compatibility shims for old `phase4_multitrack`
imports. The canonical implementation lives in `src/phases/phase4/multitrack/`.

Do not add new implementation here.
