# Midstack Validate

Use the `midstack-triage` MCP tool `midstack_validate`.

Default arguments:

- `score_min_level=medium`
- `skip_replay=false`
- `skip_score=false`

Report whether assets, replay, score gate, and Cursor MCP smoke passed.
