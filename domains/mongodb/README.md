# MongoDB Domain

本目录存放 MongoDB 专属排障资产。

当前已包含：

- 领域元数据
- 组件入口索引
- `replica-inconsistency` 场景资产
- `connection-failure` 场景资产
- `resource-exhaustion` 场景资产
- `latency-spike` 场景资产
- `data-hotspot` 场景资产
- MongoDB 第 3 段 MVP 脚本资产

后续如果增加新的资产，仍然遵循：

- 物理上按组件组织
- 逻辑上按场景检索
- 组件入口只做导航，不复制资产正文
